const fruits = ['apple', 'plum', 'pear', 'orange', 'banana'];

const firstTwoEls = fruits.slice(0, 2); // ['apple', 'plum']
const nonExtremeEls = fruits.slice(1, -1); // ['plum', 'pear', 'orange']
const lastTreeEls = fruits.slice(-3); // ['orange', 'banana']
